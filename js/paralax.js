// const parallaxContainer = document.querySelector('.parallax-container');
// const parallaxBackground = document.querySelector('.parallax-background');

// parallaxContainer.addEventListener('scroll', () => {
//   const scrollY = parallaxContainer.scrollTop;
//   const scrollX = parallaxContainer.scrollLeft;

//   const backgroundScale = 1.5;
//   const backgroundTranslateY = scrollY * (1 / backgroundScale);

//   parallaxBackground.style.transform = `translateY(${backgroundTranslateY}px) translateX(${scrollX}px)`;
// });